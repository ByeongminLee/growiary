// import { doc, getDoc } from 'firebase/firestore';
// import { db } from './firebase';

// export const getUserData = async (collection: string, docs: string) => {
//   const docRef = doc(db, collection, docs);
//   const docSnap = await getDoc(docRef);
//   return docSnap.data();
// };
